---
"radical-pipelines": patch
---

`rp stamp` stores a report's commits as full hashes and rejects ambiguous ones; `rp check` matches body hashes by prefix; frontmatter lists are read in inline form too.
