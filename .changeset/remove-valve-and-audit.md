---
"radical-pipelines": minor
---

No wave count gates a run: the valve and the audit are gone, with `--audit`, `--valve`, the `Thresholds` convention, and the `audited-*`/`episode-start-*` landing facts. `rp check` keeps the episode counter as information. The orchestrator exercises judgment only at triage and owner escalation. The upstream PR transformation cherry-picks the pipeline's own commits after its base.
