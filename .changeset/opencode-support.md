---
"@automattic/radical-pipelines": minor
---

Add opencode v2 support: a plugin exposing the `rp_*` tools, a per-tool convention file with the opencode Team spawning, Health monitoring, and Agent models blocks, and a pinned, hermetic integration suite that exercises the plugin against the pinned opencode build.
