---
"@automattic/radical-pipelines": patch
---

After a context compaction, following the skill again as at first sight includes reading anew every file it leads to. A session that took the summary's account of what it had read went on from the summary alone: it guessed the skill's location and the script's options instead of loading the conventions and the workflow.
