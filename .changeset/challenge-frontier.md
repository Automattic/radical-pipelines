---
"@automattic/radical-pipelines": minor
---

A pending challenge is the frontier only when its target's inputs are each fresh with a current valid wave; otherwise the phase walk reaches the target first and its re-synthesis carries the challenge, so every target adjudicates once, against approved inputs
