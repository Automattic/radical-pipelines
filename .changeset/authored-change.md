---
"@automattic/radical-pipelines": minor
---

BREAKING: Cover every authored change outside the pipelines folder through phase-review packages, including work made outside tasks. Bind each occurrence's provenance and byte-preserving patch material with file pins; retain both sides of changed files and a checkpoint that preserves coverage after state integration and on continuation. Resolve recorded observations uniformly across reports and packages, preserving ordered duplicates. Honor configured merge drivers when computing authored merge changes. Add `rp diff` for Fresh/Delta material and net upstream integration. Record task reports' occurrence pins as landing facts. Remove the unclaimed-commit frontier; retain `head` for artifact delta reviews and convergence. Stamps recording authored changes take `--base`. Merge closure integrates the pipeline into its artifact base in both storage modes.
