---
"@automattic/radical-pipelines": patch
---

Setup writes only the defined conventions into `.rp.md` — anything beyond them, like orchestrator instructions or setup-time discoveries, is captured only on explicit owner request. The Claude Code canonical block drops instructions the workflow already states.
