---
"@automattic/radical-pipelines": patch
---

A failed report targets its task; trigger state is per target artifact; a contradicts-input disposition left neither corroborated nor defeated is a must-fix; `rp check` validates `target-identity` against `target`
