---
"@automattic/radical-pipelines": patch
---

Every direction the owner gives when a run starts or resumes — the run policy as confirmed (the `--lanes` declaration, target phase, thresholds) and any other instruction — is quoted as a decision in `intent.md` before anything is dispatched. Ids are never reused: a plan produced anew continues the numbering.
