---
"@automattic/radical-pipelines": patch
---

Prose and templates say exactly what `rp check` pins: a consolidated artifact pins every lane's artifact, record, and approving reviews — and the lanes close only once it does, the record included; the document plan pins the spec, design doc, and build plan with their approving reviews and the approving build review. Consolidate and plan-producer materials list the same files.
