---
"@automattic/radical-pipelines": patch
---

Align the documentation with the tooling: the `rp` README describes every flag as it behaves (no `--force`; `--lanes` per artifact with fingerprints; `head` only with pins; outcomes with `blocked`; `--base`; the phase range), and the README and glossary record the commit claim, the `blocked` outcome, the artifact base, and lane identity.
