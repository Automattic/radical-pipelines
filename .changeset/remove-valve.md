---
"radical-pipelines": minor
---

The valve is gone: no wave count stops a run. The audit remains — every `audit` waves of an episode (default 3), annotated with the episode's recurring findings — and stops the run only for a pattern the orchestrator names. The upstream PR transformation cherry-picks the pipeline's own commits after its base.
