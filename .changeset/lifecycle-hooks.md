---
"@automattic/radical-pipelines": minor
---

Add the Lifecycle hooks convention — prose instructions the orchestrator runs at defined pipeline moments — and owner-invoked closure actions bracketed by before/after hooks, in which the orchestrator opens the pipeline's PR in both artifact-storage modes. Generalize the assisted close-out to fire on any stop, and migrate this repository's Linear sync onto the mechanism.
