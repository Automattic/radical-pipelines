---
"@automattic/radical-pipelines": minor
---

Add the Lifecycle hooks convention — prose instructions the orchestrator runs at defined pipeline moments, with owner-invoked closure actions (open the PR, record the merge, close without merging) — and migrate this repository's Linear sync onto it.
