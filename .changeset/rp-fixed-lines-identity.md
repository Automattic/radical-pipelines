---
"radical-pipelines": patch
---

`rp`: fixed lines are mirrored whole or rejected as `INVALID` (no token mining); identities computed in process; a `---` block not at byte 0 is `INVALID FRONTMATTER`; an attempt without a report does not exist; the stamp runs before an agent is terminated and a rejection returns to its author. The adjudication-audit rule in reviewers is a finding, not a must-fix.
