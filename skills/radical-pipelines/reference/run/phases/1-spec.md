# Phase 1 — Spec

> v3 skeleton stub — content pending. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Only what differs from the generic machine in `../loop.md`: the phase's artifacts (`spec.md`, `spec-research.md`), its profiles (`spec-producer`, `spec-reviewer`, `researcher`), the materials each template carries per mode, and lane configuration.
