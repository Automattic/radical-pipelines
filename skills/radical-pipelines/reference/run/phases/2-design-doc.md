# Phase 2 — Design doc

> v3 skeleton stub — content pending. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Only what differs from the generic machine in `../loop.md`: the phase's artifacts, its profiles, materials per mode, lane configuration — and the open-assumption register accounting duty: the design doc accounts for every open spec assumption (closes it by reading, or carries it).
