# Phase 4 — Document

> v3 skeleton stub — content pending. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Only what differs from the generic machine in `../loop.md`: the document-plan artifact loop (`document-plan-producer`, `document-plan-reviewer`); `document-worker` task execution with trailers; the batch review against the current plan (`document-reviewer`) and the document summary.
