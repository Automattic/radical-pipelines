# Assisted workflow

> v3 skeleton stub — content pending; supersedes `reference/assisted-workflow.md` and `reference/assisted-phases/`. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Will contain the assisted workflow for the spec and design-doc phases: the orchestrator drives Q&A with the owner and synthesizes the artifacts itself; owner answers are recorded verbatim and attributed `owner` (they become owner territory); the owner approves before anything is committed.
