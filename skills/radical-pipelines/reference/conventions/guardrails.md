# Guardrails

> v3 skeleton stub — content pending; supersedes `reference/guardrails.md`. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Will contain the format of the Guardrails convention: project rules per agent family, how they reach agents through the prompt templates, and their evaluation semantics (producers/workers satisfy them; reviewers additionally evaluate them against the artifact and log each outcome).
