# Lifecycle hooks

> v3 skeleton stub — content pending; supersedes `reference/lifecycle-hooks.md`. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Will contain the v3 hook moments and definition format. Redesign notes: `blocker-reported` loses its event (blockers no longer stop runs); candidate successors `amendment-opened` and `owner-escalated`; `run-ended` causes become target-reached / owner cancellation / failure / non-convergence valve; open question: issue-write and pipeline-created hooks.
