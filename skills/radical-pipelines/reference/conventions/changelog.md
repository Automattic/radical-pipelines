# Conventions changelog

> v3 skeleton stub — content pending. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Will contain one entry per conventions schema version: the change (rename / new-required / new-optional / removed) and its migration action. The delta migration in setup walks only the span between a project's `conventions:` stamp and the current version.
