# Report

> v3 skeleton stub — content pending; new entry point. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Will contain:

- The read-only entry: render pipelines, their origin graph, and per-pipeline state for an issue via the script's `status`.
- No dispatch powers — inspection only.
