# Intent format

> v3 skeleton stub — content pending; supersedes `reference/intent-format.md`. Design record: [#273](https://github.com/Automattic/radical-pipelines/issues/273).

Will contain:

- The shared schema for issues, pipeline intents, and amendment records — an amendment is an intent.
- Sections: goal, constraints, context, assumptions/directions to explore; origin.
- The machine-readable origin frontmatter: issue refs (opaque, tracker-native strings), stacked-on / forked-from pipeline refs, amendment origin chains (`adjudicates`, originating verdicts).
