# Code Review — Phase 4 batch (T1–T7): APPROVED

_Pipeline: `51-guardrails-convention-v2` (issue #51 — Add a Guardrails convention to formalize deterministic code-phase verification). Batch: all seven tasks T1–T7 (commits `ee839a8`, `cda9a37`, `8e44333`, `79a84dd`, `987fb99`, `41d2944`, `b40934a`). Diff reviewed: the seven task commits (`ee839a8^..b40934a`), excluding the `.pipelines/` artifact folder._

## Verdict

**APPROVED.** All seven tasks meet their per-task Acceptance, all five cross-cutting whole-change gates pass, and every out-of-scope boundary is respected. No blocking issues, no rejection findings.

## Scope note

The batch metadata's base ref (`a2c1e04`, the run-start merge-base) now diffs as 51 files because trunk has advanced past that merge-base. The actual batch — the seven task commits — touches **exactly the eight in-scope files** the spec scopes: `load.md`, `setup.md`, the four phase agents, `.rp.md`, and one `minor` changeset. The 43 extra files in the base-ref diff are unrelated trunk drift and are not part of this batch; I reviewed the seven task commits.

## Per-task confirmation

- **T1 — `load.md` (loader row + body + local-overrides carve-out).** `## Conventions` gains a `Guardrails` row marked `No`, with a terse cell signalling executable/exit-code semantics. The table stays column-aligned. A `##`-level `## Guardrails` body section (no new `###` introduced — `grep "^### " load.md` is empty) documents all three: guardrail = exact command judged by exit code mandatory within its phase(s), valid targets `code`/`docs`; absent/empty = no command gates (never a blocker/warning); the phase-selection rule. `## Missing conventions` is untouched — Guardrails appears in no required-completeness check. The `## Local overrides` carve-out is a single scoping sentence ("Guardrails is shared and committed-only; it is never taken from `.rp.local.md`"), with no "ignored/warn-on-override" enforcement and no enumerated overridable subset. (AC 1, 2, 11, 13.)
- **T2 — `code-writer.md`.** Step 1 reads "the guardrails applicable to the code phase"; step 5 (renamed "Run the code-phase guardrails") runs every code-phase guardrail, mandatory, no bypass, no invented commands. The three-way split is explicit (absent ⇒ run none and proceed; declared-but-unrunnable ⇒ blocker / drift guard; runs-but-nonzero ⇒ work). "Failing gates are work, not blockers" is re-anchored beside the nonzero branch (not orphaned). The spine appears. Behavior verification (step 3) stands alone, drives the behavior itself, decides the evidence, and is explicitly "not a guardrail." Leave-alone refs (`:29` inline API doc, `:38` UI, `:42` testing, commit format) intact verbatim. (AC 7, 8, 9, 10.)
- **T3 — `code-reviewer.md`.** Step 1.5 reads code-phase guardrails; the "No regressions / verification gates pass" check runs every code-phase guardrail and records each command + result in the Checks table; the blocker guideline drops "verification convention is undefined" and adds the declared-but-unrunnable blocker; behavior verification decoupled with the "no evidence ⇒ reject" rule kept; review-template comment de-anchored. Leave-alone refs (`:30` inline API doc, `:31` coding/testing/build, commit format) intact. (AC 7, 8, 9, 10.)
- **T4 — `doc-writer.md`.** Straggler fixed ("if a docs-phase guardrail covers doc tests"); step 4 reads docs-phase guardrails and keeps the link-check / markdown-lint / render-check / doc-tests / spelling examples; three-way split present; "Failing doc gates are work" re-anchored. The distinct **documentation convention** (`:17`/`:27`, voice/structure) and commit format left verbatim — naming-collision discipline honored. (AC 7, 8, 10.)
- **T5 — `doc-reviewer.md`.** The "Doc gates" check rewritten to "Docs-phase guardrails" (run each, record in Checks table; none ⇒ accuracy spot-check is sole gate); the "run the gates" guideline and the blocker guideline rewritten with the three-way split (dropping "verification convention is undefined," adding the declared-but-unrunnable blocker). Documentation convention (`:19`/`:32`) and commit format left verbatim. (AC 7, 8, 10.)
- **T6 — `setup.md`.** A bare `### Guardrails` capture sub-section (no `(required)`/`(optional)` suffix) is placed **last** in Step 2, after `### Artifact storage (required)`. It explains the backpressure rationale and the kinds of gates, captures (name, exact literal command, phase(s) ∈ {code, docs}), accepts "None" as complete, validates each command **as captured** (before the Step-4 confirm), and states the full three-way outcome split (runs-but-nonzero ⇒ WRITE with pass bar "it executed"; unrunnable 127/126 ⇒ don't-write + surface + fix/drop/insist-escape-hatch, default don't-write, never "write anyway but warn"; zero captured ⇒ nothing to validate). Per-command independence, exit-code-as-heuristic / owner-arbitrates-ambiguous, the main-checkout parity floor (explicitly "not a worktree working directory"), both edge-case caveats (hang/no-return folded into don't-write with no mandated timeout number; state-mutating command confirmed-before-running), and the spine are all present. (AC 4, 5, 6.)
- **T7 — `.rp.md` dogfood + changeset.** `### Guardrails` under `## Shared conventions` declares the three real code-phase gates (`npm test`, `node scripts/validate-changesets.mjs`, `npx changeset status --since=origin/trunk`) as a `Name | Command | Phase` table mirroring `### Agent models`, with the one-line Node ≥21 note. File title unchanged; no `## Guardrails` sibling; no "configuration" retitle. The `npm test` script is not modified. A new `.changeset/guardrails-convention.md` carries `"@automattic/radical-pipelines": minor` with a one-paragraph summary, matching the `pipeline-reviews.md` template shape; the changeset is the thing checked, not declared as a guardrail. (AC 3, 12, 14.)

## Cross-cutting whole-change gates (code-plan lines 316–325)

1. **Grep-negative (AC 10 / req 23).** `grep -rn "verification convention" agents/` returns nothing (exit 1). No shipped file outside `.pipelines/` contains the string. All leave-alone convention references are intact in all four agents; the doc "gates" are rewritten to "docs-phase guardrails." **PASS.**
2. **End-to-end optionality (AC 13 / req 26).** Setup capture accepts "None"; the loader's required-completeness check is unchanged (the `No` row is structurally invisible to `## Missing conventions`); all four agents encode absent/empty ⇒ run none and proceed with no "missing = blocker" path surviving (verified by targeted grep). No blocker and no warning anywhere across setup + load + agents. **PASS.**
3. **Out-of-scope respected (AC 15 / OOS 1–9).** No "configuration" rename/retitle/sibling section; `npm test` script unchanged; no phase-reference-doc edits (`reference/autonomous-phases/`, `reference/assisted-phases/` untouched by the batch); no per-tool guardrail variants; no parser/schema (prose table only); no new local-overrides mechanism or warn text; no README/website prose; no back-fill into other `.rp.md` files. **PASS.**
4. **Dogfood gates green (AC 12 / req 25).** On Node v24.15.0 (CI-parity, Node ≥21): `npm test` exit 0 (22 tests pass), `node scripts/validate-changesets.mjs` exit 0, `npx changeset status --since=origin/trunk` exit 0 (accepts the new `minor` changeset). On local Node v20.19.4 the two changeset gates still exit 0; `npm test` exits 1 with the `**`-glob "Could not find" error — the **documented environment-parity case** (req 25 / AC 12, spec OOS 2 forbids fixing it), recorded as expected, not a batch defect. **PASS.**
5. **Changeset present and not a guardrail (AC 14 / req 27).** One `minor` changeset; not declared in `.rp.md`'s `### Guardrails`. **PASS.**

## Evidence

- Grep-negative: `grep -rn "verification convention" agents/` → exit 1 (empty); no shipped `*.md` outside `.pipelines/` matches.
- Dogfood gates (Node 24, CI parity): `npm test` → exit 0 (`pass 22, fail 0`); `node scripts/validate-changesets.mjs` → exit 0; `npx changeset status --since=origin/trunk` → exit 0.
- Dogfood gates (local Node 20): changeset gates exit 0; `npm test` → exit 1 (glob/parity case, documented).
- Structural checks: `load.md` has no new `### ` heading; `## Missing conventions` unchanged; `.rp.md` has a single `## ` section (`## Shared conventions`) with `### Guardrails` under it, original title intact, no `## Guardrails` sibling; `setup.md` `### Guardrails` is the last Step-2 sub-section with a bare heading.
