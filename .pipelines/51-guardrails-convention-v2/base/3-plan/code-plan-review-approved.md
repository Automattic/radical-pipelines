# Code Plan Review — Approved

_Pipeline: `51-guardrails-convention-v2`. Reviewed: `3-plan/code-plan.md` against the binding `1-spec/spec.md`, the approved `2-design-doc/design-doc.md`, the `2-design-doc/design-doc-review-approved.md`, and the live worktree. Iteration N=1._

## Verdict: approved

## Summary

The code plan is a complete, feasible, and faithful translation of the approved design into seven file-disjoint tasks, and every load-bearing claim it makes about the codebase reproduces against the live tree. The plan covers all 27 spec requirements and all 15 acceptance criteria with traceable tasks, carries the grep-negative hard gate as a cross-task invariant, honors the leave-alone / naming-collision discipline, and correctly incorporates the three non-blocking design-review observations. I independently re-verified the full line map, the file structures the tasks edit against, the live dogfood-gate behavior, and the changeset mechanics — all hold. The two observations below are non-blocking polish notes for the code phase, not grounds for rejection.

## What I verified (and it held)

- **Grep surface (spec AC 10 / req 23).** `grep -rn "verification convention" agents/ skills/ .rp.md` matches in exactly the four phase agents (17 occurrences) and nowhere in `skills/`, `.rp.md`, or the phase-reference docs (`autonomous-phases/`, `assisted-phases/` clean for both "verification convention" and "guardrail"). Greenfield, exactly as the plan's Grounded-state section claims.
- **Agent line map (T2–T5).** Every cited pre-edit line anchor matches the live file content: code-writer `:13/:36/:46/:50/:51/:70` and leave-alone `:29/:38/:42/:56`; code-reviewer `:18/:32/:36/:68/:98` and leave-alone `:30/:31/:84`; doc-writer `:35/:40/:42/:43/:44/:45/:65` and leave-alone `:6/:27/:50`; doc-reviewer `:33/:98/:99` and leave-alone `:19/:32/:85`. The naming-collision trap (doc "gates" at doc-writer `:40` / doc-reviewer `:33` ARE docs-phase guardrails; the documentation *convention* at doc-writer `:6/:27` and doc-reviewer `:19/:32` stays) is correctly distinguished in T4/T5.
- **Loader structure (T1).** `load.md` has no `###` headings (all `##`-level), the `## Conventions` table is at `:11-21`, `## Missing conventions` at `:23-29` keys off required conventions only, and `## Local overrides` ends with the unconditional merge rule at `:37`. The plan's hard constraints — `No` row stays out of every required-completeness check, body is `##`-level not `###`, carve-out is a scoping sentence not warn-on-override — are all correct and grounded.
- **Setup structure (T6).** Step 2 ends with `### Artifact storage (required)` (`:106-169`), Step 4 confirm-before-write is at `:179-186`, and `setup.md:3` confirms setup runs before any worktree exists — so the main-checkout parity floor (binding per spec req 17, not the worktree) is correctly grounded. The `(optional)`-suffix-vs-bare-heading observation is correctly resolved toward a bare `### Guardrails`.
- **`.rp.md` shape (T7).** `## Shared conventions` holds tool-agnostic `###` subsections including `### Commit format`, `### Agent models`, `### Health monitoring`; the title `# Radical Pipelines project conventions` is unchanged. The placement instruction is actionable.
- **Dogfood gates live (T7, spec AC 12 / req 25).** On this Node v20.19.4 worktree: `npm test` → exit 1 ("Could not find '…/scripts/test/**/*.test.mjs'", the `**`-glob parity case), `node scripts/validate-changesets.mjs` → exit 0, `npx changeset status --since=origin/trunk` → exit 0 (`origin/trunk` resolves to a real commit). CI uses Node 22 (`changeset-gate.yml:26`) and runs these exact three steps (`:31/:34/:37`, CI varying the base via `github.event.pull_request.base.ref`, dogfood pins `origin/trunk`). The Node ≥21 caveat is genuine, and the plan correctly refuses to fix the glob wart (OOS 2).
- **Changeset mechanics (T7, spec AC 14 / req 27).** Package `@automattic/radical-pipelines` at `0.2.0`; `.changeset/config.json` `changedFilePatterns` includes `skills/**` and `agents/**`, `baseBranch` is `trunk`; `validate-changesets.mjs` forbids `major` pre-1.0 so `minor` is correct; `.changeset/pipeline-reviews.md` matches the described template shape. The authored-not-a-guardrail framing dissolves the apparent circularity.
- **Review observations incorporated.** All three design-review observations are absorbed: no new `###` in load.md (T1 change 2 + acceptance 4), bare setup heading (T6 change 1), and re-anchor — not orphan — the "failing gates are work" sentence (T2 change 2.3, T4 change 2).

## AC-to-task coverage

AC 1–2 → T1; AC 3 → T7; AC 4–6 → T6; AC 7–10 → T2/T3/T4/T5 + cross-cutting grep-negative; AC 11 → T1; AC 12 → T7; AC 13 → cross-cutting (T1 + T6 + T2–T5); AC 14 → T7; AC 15 → cross-cutting. Every acceptance criterion maps to at least one task with concrete, checkable acceptance assertions.

## Non-blocking observations (for the code phase, not grounds for rejection)

1. **code-reviewer `:97` "Run the verification gates" guideline.** This Guidelines bullet ("**Run the verification gates.** Don't just read the code…") does not contain the literal string "verification **convention**," so the grep-negative gate (AC 10) is unaffected whether or not it is touched, and "verification gates" remains technically accurate (guardrails *are* verification gates). It is on neither the spec's leave-alone list (req 23) nor the plan's T3 change list, so the writer has latitude. It reads mildly stale once the convention it implicitly referenced is gone; the writer may want to align it to "Run every code-phase guardrail" for consistency with the rewritten `:32`. A polish note, not a defect — the plan's T3 is not wrong for omitting it.

2. **`.rp.md` shared-conventions enumeration is partial.** The plan (inheriting the design) names three `### Shared conventions` subsections (`### Commit format` / `### Agent models` / `### Health monitoring`), but the live file has more (`### Managing tasks`, `### Pipeline slugs`, `### Artifact folders`, `### Worktrees`, `### Branch names`, `### Team spawning` in addition). This does not affect the task: the new `### Guardrails` still goes under `## Shared conventions` alongside the named subsections, all of which exist. The placement instruction remains fully actionable; the partial list is a grounding imprecision, not an error.

Neither observation changes a task, contradicts the spec/design, or blocks execution. The plan is complete, feasible, internally consistent, and faithful to the binding spec and approved design. Approved.
