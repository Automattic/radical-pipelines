# Code Plan Review

## Verdict: approved

## Summary

The revised plan resolves the prior rejection's sole blocking issue and holds up under re-review. The earlier defect was that Tasks 2 and 3 gave a self-contradictory placement instruction ("near the `## Worktrees` content" while also "not inside the `.rp.md` canonical block") — mutually exclusive, because in both tool-convention files the entire worktree section lives inside the fence. The writer resolved it by placing the worktree-root derivation **inside** the fenced `.rp.md` block, in each file's worktree section, on the precedent that the existing per-tool command completing the same `health-monitoring.md:13` generic deferral (the `/loop` command) sits inside the fence. This diverges from the prior reviewer's suggested out-of-fence fix, so I evaluated it on its merits against the actual files rather than against that suggestion. The chosen placement is correct, decisive, and precedent-faithful. Coverage, traceability, ordering, scope discipline, the `None` guardrail rendering, the inspection-only E2E framing, the absence of unit-test and documentation planning, and the by-construction preservation of Requirement 7 all remain sound. I verified every load-bearing structural claim against the codebase.

## Verification performed

- **Fence boundaries and the `/loop` precedent (the crux of the revision).** In `claude-code.md` the fence opens at line 9 (` ```markdown `) and closes at line 44 (the file's last line); the `## Worktrees` section (lines 10-16) and the `## Health monitoring` section with its `/loop 5m <prompt>` command (line 39) plus the orchestrator-only note "The orchestrator starts the loop itself; the owner is not asked to run the command" (line 43) all sit **inside** the fence. There is no out-of-fence prose at all. In `pi.md` the fence opens at line 7 and closes at line 41; the `## Pi worktrees` section (lines 8-16, including the `cwd` convention at line 16) and the `## Health monitoring` `/loop` command (line 36) and its orchestrator-only note (line 40) all sit **inside** the fence; the only out-of-fence region is the unrelated `## Setup actions` section (lines 43-65). The line numbers cited in Tasks 2 and 3 match exactly. The precedent claim is true: the per-tool command completing the `health-monitoring.md:13` deferral lives inside the fence in both files — and that fenced content already carries orchestrator-only operational instructions, so the inside-fence placement of the worktree-root derivation is consistent with how the fence is actually used, not merely asserted.
- **The generic-deferral precedent.** `health-monitoring.md:13` reads "The active tool's rules (see `conventions/claude-code.md` or `conventions/pi.md`) provide the exact slash command to start the loop." Task 1's instruction to mirror this phrasing in `passing.md` while keeping it tool-mention-free is faithful to the precedent and to the authoring rule.
- **Committing-agent classification.** `spec-researcher.md` and `design-doc-researcher.md` contain no commit step; their paired analysts commit the research artifacts (`spec-analyst.md:82`, `design-doc-analyst.md:67`). Every other role (`spec-writer`, `spec-reviewer`, `spec-consolidator`, `code-plan-writer`, `code-reviewer`, `docs-reviewer`, etc.) has its own commit step. The plan's "committing agents = all roles except those two researchers" is exactly correct and matches the design.
- **Agent-profile surface.** 18 agent profiles exist, matching the design's "~18 profiles untouched." The "takes effect against the worktree — judge before running it" prose that Task 4 says it must not contradict is present in `code-plan-reviewer.md` and `docs-plan-reviewer.md`, as the plan claims.
- **`passing.md` shape.** The `## Conventions` block uses the `- **Label:**` + nested `Agents:` style the plan extends, and the existing `Guardrails` field already scopes a subset of agents — the precedent Task 1 invokes for scoping the branch field.

## Re-review of the prior rejection's issues

- **Issue 1 (blocking) — placement was unsatisfiable.** Resolved. Tasks 2 and 3 now state a single placement (inside the fence, in the worktree section) with verified line-number anchors and no competing "not inside the block" clause. The decision is single-valued; two code-writers would place the text identically. The choice is precedent-correct: the same `health-monitoring.md:13` deferral's existing per-tool completion sits inside the fence, including orchestrator-only operational notes, so the fenced block is the established home for this kind of content. Inside-fence is also the only non-inventive option for `claude-code.md`, which has no out-of-fence region.
- **Issue 2 (minor, fold-in) — acceptance did not pin the fence-boundary outcome.** Resolved. Both tasks' Acceptance bullets now assert a fence-relative, observable criterion: the new statement "appears inside the fenced `.rp.md` canonical block (between the opening ` ```markdown ` and the closing ` ``` `), within the `## Worktrees` / `## Pi worktrees` section," matching where the `/loop` command sits — and, for `pi.md`, not in the out-of-fence `## Setup actions` section. These are checkable by line position.

## Other dimensions

- **Coverage.** Every spec acceptance criterion maps to at least one task and at least one E2E flow (AC 1 via the union of Flows 2/3/4/7; AC 2-8 individually). Every key design decision is executed (two-part design, central carriage in the block, asymmetric verify form, the all/committing scoping split, the generic-instruction-with-tool-defer, and Requirement 7 by construction).
- **Guardrail scopes.** No guardrails are defined and no scoped gate was passed, so the single `| None | — |` row is the valid rendering and there is no filled command to execute.
- **Ordering and feasibility.** Task 1 (generic field) precedes Tasks 2/3 (per-tool derivations that complete it), which precede Task 4 (end-to-end inspection). All referenced files, sections, and line ranges exist as described.
- **Scope and authoring discipline.** The plan stays within the spec and design, prescribes no unit tests (correct — prose, no harness; `CLAUDE.md` forbids structural tests over skill/agent content), includes no documentation tasks, adds no Requirement 7 carve-out language, and records the unverified Pi-tooling specifics as a flagged note with deterministic fallback wording rather than a blocker.
