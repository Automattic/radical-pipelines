# Spec Review

## Verdict: approved

## Reviewer

Owner (assisted workflow)

## Notes

Approved as drafted. Scope is defined by exclusion (all output outside `<artifacts-folder>`); the discriminator is referent-based (a pointer to a specific artifact of this run) and applied uniformly with no self-hosting carve-out; the rule wording avoids "pipeline"; producers carry a Guidelines disposition and reviewers a detection check; enforced at the existing per-phase review gate. Rule 1 (leave untouched comments untouched) is deferred.
