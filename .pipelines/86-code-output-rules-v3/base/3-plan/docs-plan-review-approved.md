# Docs Plan Review

## Verdict: approved

## Summary

The docs plan is correct, complete, and drift-resistant for this change. The change is a prose-only edit to five agent profiles under `agents/`: three producers gain a `## Guidelines` disposition, two reviewers gain a `### 2. Review the changes` detection item, and one pre-existing narrower line is deleted from `code-writer-tdd.md`. I independently verified the plan's load-bearing claim — that no external/host-project documentation surface reproduces the agents' internal Guidelines dispositions or review-checklist items — by an end-to-end sweep of the repository, and it holds: the producer Guidelines bullets, the reviewer checklist labels, and the deleted comment line appear only inside the agent profiles themselves, never in `README.md`, `website/`, `CONTRIBUTING.md`, `AGENTS.md`, `.rp.md`, the `skills/` reference files, `.claude-plugin/`, `.github/`, or the nested `.changeset/README.md`. Consequently, adding a bullet to five profiles leaves no external prose stale, and the single warranted documentation surface is the repository's committed changeset. The plan's one task (add a changeset) is correct and sufficient: `.changeset/config.json`'s `changedFilePatterns` includes `agents/**`, so the Changeset Gate's presence check requires a changeset on this PR, and the repository's authoritative bump table maps a backwards-compatible behavior addition to a non-empty `minor` bump pre-1.0 — exactly what the plan prescribes (not empty, not breaking). The `## Guardrail scopes` section correctly renders `None`/`None` because `.rp.md` declares no Guardrails convention and no gate was passed to this phase.

## Verification performed

- **Guardrail scopes.** `.rp.md` contains no `## Guardrails` section and declares no fixed or scoped gates. No scoped gate exists, so none could be passed to this phase; the plan's `None`/`None` table is the valid rendering. There are no filled commands to execute, so per-command validation is vacuously satisfied.
- **Surface sweep (central claim).** Grepped the product surfaces (`README.md`, `CONTRIBUTING.md`, `AGENTS.md`, `.rp.md`, `website/`, `skills/**`, `.claude-plugin/`, `.github/`, nested READMEs) for: the deleted line's phrasing ("self-contained", "never reference"); the reviewer checklist labels (`Per-task Acceptance coverage`, `Convention compliance`, `Review the changes`, etc.); the reviewers' enforcement posture ("reject liberally", "must-fix"); and the rule concept ("written by hand", "point back", "reference the run/spec/plan"). No product surface reproduces any producer Guidelines disposition, any reviewer checklist item, or the deleted comment line. `README.md:112` lists the affected agents only as a flat roster of profile names; `website/demo.js:103` has a benign hand-written-style commit example with the exempt agent-name tag (`Add orchestrator (code-writer-tdd)`) that neither states nor violates the rule; the phase-4 and phase-5 skill references describe only artifact paths and phase mechanics; `CONTRIBUTING.md` and `AGENTS.md` carry only release/authoring mechanics; the `.changeset/README.md` cheat-sheet is unaffected.
- **Profile structures exist.** Confirmed the surfaces the code plan edits are real: `## Guidelines` lists in all five profiles, the deleted line at `agents/code-writer-tdd.md:33` inside the "Document every public symbol" block, and the reviewers' `### 2. Review the changes` → "Check, for the tasks in this batch:" checklists with `Convention compliance` as a peer item. The docs plan's dependency on Code Tasks 1–5 is grounded.
- **Changeset warrant and bump type.** `.changeset/config.json` `changedFilePatterns` includes `agents/**`; `CONTRIBUTING.md` (§"The changeset gate (CI)", §"When a changeset is required", §"Bump types", §"Pre-1.0 policy") confirms the presence check fails a release-relevant PR without a changeset, that `minor` is the pre-1.0 mapping for a backwards-compatible feature/addition, that `major` is forbidden pre-1.0, and that an empty changeset is reserved for prose-only no-op edits. The package is `private`, version `0.5.0` (pre-1.0). `website/**` is correctly non-release-relevant. The plan's bump guidance resolves to `minor` and its acceptance criteria correctly exclude empty/no-op and breaking.

## Assessment against review criteria

- **Guardrail-scopes coverage and binding:** Correct — no scoped gate to fill; `None` body is the valid rendering; no unbound or missing rows.
- **Coverage of surfaces:** Complete — the only surface the shipped change leaves warranting documentation is the changeset, and the plan covers it. The "Surfaces reviewed and intentionally not changed" section accurately records every swept surface with a correct rationale for leaving it untouched.
- **Traceability:** Task 1 traces to Code Plan Tasks 1–5 (the `agents/**` edits that trigger the changeset requirement) and to Spec R1/R9 as the behavior the summary describes.
- **Per-task acceptance:** Four evaluable, drift-resistant criteria (one committed changeset so the gate passes; valid front matter + non-empty imperative summary conveying the user-facing behavior; correct bump type; the summary is itself clean host-project output). No frozen filename or summary wording is hardcoded.
- **Drift-resistance:** The task stays at what/where/for-whom; it prescribes no exact summary text and no function/parameter details.
- **Audience clarity:** Concrete — release-notes/changelog and GitHub Release readers who consume the profiles as a plugin/package, distinguished from profile editors.
- **Granularity, ordering, feasibility:** A single small task with no dependencies; `.changeset/` exists and the changeset workflow is documented in `CONTRIBUTING.md`.
- **No code planning; scope:** The plan contains no code tasks and stays within the spec and design.
- **Clarity and consistency:** Two docs-writers executing this plan independently would produce the same single changeset of the same scope and shape.
