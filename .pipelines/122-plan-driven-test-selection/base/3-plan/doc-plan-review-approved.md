# Doc Plan Review — APPROVED

_Issue 122 (plan-driven test selection and reviewer-side behavior verification). BASE run, branch stacked on #121. Reviewed `3-plan/doc-plan.md` adversarially against `1-spec/spec.md`, `2-design-doc/design-doc.md`, and `3-plan/code-plan.md`._

## Verdict

**APPROVED.** The doc plan covers exactly the docs-phase obligations — a single new `minor` changeset for issue 122 — and correctly defers everything else. The whole-repo sweep is sound, all citations match live HEAD, and nothing strays into code-plan territory.

## Checks performed

### 1. Scope — exactly the docs-phase obligations, no more

- **One task only** (the changeset). Confirmed.
- **README agent-roster is NOT re-planned here.** The plan attributes it to code-plan **T6**, and that is accurate: code-plan Task 6 (L131/L136) explicitly edits `README.md` L112 to replace `code-writer` with `code-writer-tdd`, `code-writer-e2e`. The doc plan does not duplicate it. Correct.
- **CHANGELOG.md treated as immutable** (regenerated by `changeset version` at release; never hand-edited by a feature PR). Correct.
- **`.rp.md` self-edit deferred** as operational follow-up — matches spec "Out of Scope" (adding writer model rows / dropping the `code-writer` row is project config, not a skill change). Correct.

### 2. Changeset task specification

- **`minor` bump**, package key `"@automattic/radical-pipelines"` — verified against `package.json` `name` (exact match) and against #121's existing changeset which uses the same key.
- **Distinct slug** required (e.g. `plan-driven-test-selection`), with `.changeset/agent-scoped-guardrails.md` left byte-for-byte untouched. Verified #121's file exists at HEAD (1154/345 bytes) and is a separate feature's `minor` changeset.
- **Bump justification** cites the authoritative `CONTRIBUTING.md` bump table verbatim: `minor` = "New features; backwards-compatible additions"; pre-1.0 forbids `major`. Verified against live `CONTRIBUTING.md` L89-97. The change is a genuine additive feature (not a prose-only edit), so `minor` is correct, not `none`.
- **Release-note altitude** mandated; **mechanism restatement explicitly forbidden** (two-question discipline, per-command validation, four-step writer shape, `Type` dispatch, section shapes, reviewer re-drive) — invoking `AGENTS.md`'s no-duplication rule, which is grounded (verified `AGENTS.md` L8/L9/L13). No reference to internal selection rules. Correct.
- **Acceptance criteria** are concrete and verifiable: validates via `node scripts/validate-changesets.mjs` (verified the script exists at HEAD) and `npx changeset status` presence gate. Correct.

### 3. Whole-repo sweep justification — sound

Every human-facing surface is accounted for, each absence an explicit decision:
- **README.md** — owned by code-plan T6 (verified); phase-4 line L31 ("unit and end-to-end tests") stays literally true (both writers collectively produce both). Verified L31 and L112 at HEAD.
- **CONTRIBUTING.md** — governs Task 1 (bump authority), not stale: keyword sweep found no `code-writer` / behavior-verification / test-selection / e2e references. Verified at HEAD.
- **AGENTS.md** — constrains how prose is written; nothing the feature adds is described there. Verified.
- **website/** — only generic "agents" framing; no writer-roster / verification / `code-writer` references. The `15 agents shipped` hero stat (verified `index.html` L108) does not track the raw file count (verified `agents/` already holds 17 files), so a rounded marketing figure that net +1 does not newly falsify. `website/**` is also not release-relevant (verified `changedFilePatterns` in `.changeset/config.json`). Touching it would be an unscoped human marketing decision. Sound.
- **CHANGELOG.md** — immutable; verified exists at HEAD. Correct.
- **doc-writer.md / doc-reviewer.md** — unaffected; `doc-writer.md:64`'s `code-writer` is an incidental example, not a roster/dispatch claim (verified at HEAD). Correct.
- **SKILL.md:39** — "behavior verification" stays a phase-4 output (now produced at the reviewer); verified L39 still true. Autonomous `3 - plan.md` carries no test-planning content (verified — only a completion-predicate `code-plan.md` mention). Correct.
- **#121's changeset and `.changeset/README.md`** — frozen / non-feature cheat-sheet (verified auto-generated header). Correct.

### 4. Citations match live files

All spot-checked via `git show HEAD:<file>`: package name, changeset config patterns, `validate-changesets.mjs`, CONTRIBUTING bump table, README L31/L112, `agents/` file count (17), website agent stat (15), SKILL.md:39, doc-writer.md:64, AGENTS.md no-dup rule. Every citation is accurate.

### 5. No code-plan territory crossed

The plan repeatedly and correctly defers the entire functional documentation surface (the two planner sections, inverted rules, reviewer duties, writer split, reviewer re-drive, phase-4 dispatch, load/setup lockstep, README roster, assisted phase-3) to code-plan T1–T7. The "Surfaces deliberately not given a task" section makes each deferral explicit. No overlap.

## Notes (non-blocking)

- Overview L9 frames the release-relevant paths as `skills/**` + `agents/**` + `README.md`; the changeset file itself lands under the non-release-relevant `.changeset/` path. This is stated correctly — the PR's T1–T7 edits are what trip the gate, and Task 1's body says so precisely. Not an error.
