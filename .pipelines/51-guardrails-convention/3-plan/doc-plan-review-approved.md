# Doc plan review — APPROVED

Issue #51 (Guardrails convention), Phase 3 (Plan), docs-phase plan.
Doc plan reviewed: `3-plan/doc-plan.md` (commit `71c1728`).
Reviewed adversarially against `1-spec/spec.md` (R1–R17 / AC1–AC12),
`2-design-doc/design-doc.md` (D1–D9, §9), and `3-plan/code-plan.md` (T1–T10),
with every exclusion claim independently verified against the real files.

## Verdict

**APPROVED.** The repo-wide doc sweep is complete and correct: the README
Configuration narrative is the only substantive stale human-facing surface, and
every deliberately-excluded surface was independently confirmed clean. No stale
doc surface was missed. No code tasks are included, the changeset is not
double-planned, and scope matches the spec's Out-of-Scope routing.

## Independent verification of the exclusions (the primary failure mode)

I did not take the plan's sweep on faith — I checked each claimed exclusion
against the actual file content.

- **README `:159` (and `:143`, `:145`) — stale, correctly the Task 1 target.**
  `README.md:159` reads "organized as a shared section … followed by a per-tool
  section"; `:143` calls `.rp.md` contents "shared conventions"; `:145` says
  setup "separates shared project guidance from guidance specific to the active
  agentic coding tool." All three carry the now-stale top-level framing and none
  mention guardrails. Confirmed.
- **`conventions/claude-code.md` / `conventions/pi.md` — correctly excluded.**
  Read in full. The canonical `.rp.md` content block in each contains ONLY
  tool-specific convention subsections (`## Worktrees`, `## Branch names`,
  `## Team spawning`, `## Health monitoring` in `claude-code.md`; `## Pi
  worktrees`, `## Pi branch names`, `## Pi team spawning`, `## Health monitoring`
  in `pi.md`). Neither makes any "shared section / per-tool section" structural
  claim about `.rp.md`'s top-level shape, and neither carries any guardrails
  content. They do not go stale under the `## Conventions` + `## Guardrails`
  restructure. Confirmed.
- **`pi.md:45` step-number reference — correctly routed to the code phase.**
  "Step 3 of `setup.md` installs them …" is the one stale step-number reference;
  it is owned by code-plan **T5** (renumber-proofing), not a human-facing doc
  edit. The doc plan correctly excludes it and Task 3 only confirms no residual
  numbered reference remains. Confirmed.
- **Website (`index.html`, `demo.js`) — correctly excluded.** `index.html` has
  **zero** hits for convention / `.rp.md` / guardrail / verification / shared
  section / per-tool. `demo.js:103` uses `npm test` only as a bash transcript
  prop inside a sped-up demo array, not as a documentation claim. Nothing on the
  site describes the structure that changed. Confirmed; Task 3 re-checks it.
- **`SKILL.md` — correctly excluded.** Its "Project conventions" section
  (`:42`–`:46`) only points to `reference/conventions/load.md` ("See … for the
  full list"); it makes no structural claim about `.rp.md`. The phase-4 table row
  (`:39`) listing "behavior verification" stays accurate (R15 preserves behavior
  verification). Confirmed.
- **`CONTRIBUTING.md` — correctly excluded.** Every "gate" occurrence names the
  **Changeset Gate (CI)** workflow (`changeset-gate.yml`), which spec Out-of-Scope
  and design §9 explicitly say must **not** be renamed to "guardrail." Confirmed.
- **`AGENTS.md` — correctly excluded.** No guardrail / verification-convention /
  shared / per-tool / `.rp.md` narrative present. Confirmed.
- **No hand-maintained changelog.** Only `.changeset/*.md` files exist; the
  changelog is generated by the release tooling. No manual changelog surface, so
  no changelog doc task is owed. Confirmed.

### Decisive whole-repo sweep

Grepping `shared section|per-tool section|per-tool sections` across all markdown,
then excluding the historical pipeline artifacts (`.pipelines/**`), `node_modules`,
and the agent/skill internals owned by the code phase, the **only** two
human-facing files carrying the stale top-level "shared/per-tool" structure claim
are:

1. `.rp.md:3` — owned by **code-plan T2 / design D2** (already a code-phase edit);
2. `README.md:159` — exactly the Task 1 target.

No other maintained human-facing surface carries the stale framing. The doc
plan's central completeness claim holds. (The `.changeset/*.md` files that mention
`.rp.md` are immutable historical release notes for prior features, not a
maintained structural-description surface — correctly left untouched.)

## Other checks

- **No code tasks.** Task 1 (README narrative), Task 2 (changeset/changelog
  reconciliation — no new file), Task 3 (accuracy sweep). None are code-phase work.
- **Changeset not double-planned.** Task 2 explicitly forbids authoring a second
  changeset; code-plan **T10** owns the single `"@automattic/radical-pipelines":
  minor` changeset that covers README.md. Verified `.changeset/config.json`
  `changedFilePatterns` includes `"README.md"`, so Task 2's premise (the code-phase
  changeset already represents the README edit) is factually correct. Task 2 is a
  verification/reconciliation task, not an authoring task — the right division.
- **Per-task acceptance is outcome-based and drift-resistant.** Acceptance is
  phrased over coverage/outcomes ("no human-facing surface describes …",
  "introduces the Guardrails concept accurately: exact command, exit-code
  pass/fail, mandatory, code and/or docs, tool-agnostic, optional", "all
  cross-references resolve") rather than exact wording or symbol names. Line
  numbers are declared up front as text-matched anchors, not contracts.
- **Scope matches the spec's Out-of-Scope routing.** Spec routes README, website,
  and per-tool rule files to the docs phase; the plan documents the README (Task 1)
  and evaluates-and-excludes the website and per-tool files with a verification
  pass (Task 3).
- **No invented out-of-scope documentation.** The Guardrails pointer links to the
  `load.md` canonical definition (code-plan T3, which adds a `## Guardrails`
  section → a valid `load.md#guardrails` anchor); Task 3 guards that the
  cross-reference resolves. CI "gate" naming is explicitly preserved.

## Minor note (non-blocking, recorded for the doc-writer)

- **Task 1 Sections-scope line-label slip.** doc-plan.md:124–128 calls `:147` a
  "local-overrides sentence." In the current README, `:147` is the per-tool
  *conventions* catalog sentence ("Shared project conventions include … / Claude
  Code conventions add … / Pi conventions add …"); the actual local-overrides
  sentence is `:149`. This is a label slip, not a scoping error: the catch-all
  instruction ("touch them only where they assert the 'shared/per-tool
  conventions-only' structure and would otherwise contradict the new model") and
  the outcome-based acceptance ("per-tool framing is kept only where it correctly
  applies to **conventions**") still produce the correct result — the per-tool
  split *of conventions* is preserved (spec Out-of-Scope, design D1), so only the
  top-level structure claim at `:159` and the omission of guardrails must change.
  No outcome changes; flagged only so the doc-writer matches on text, not the
  `:147` label.

Approved.
