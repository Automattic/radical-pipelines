# Docs Plan Review

## Verdict: approved

## Summary

The plan is correct, complete, drift-resistant, traceable, and feasible. It identifies a single in-scope documentation surface — the run-model paragraph at `README.md:157` — and scopes it to a precision edit that renames only the run-naming tokens (the `review-N-<short-description>/` follow-up-run name and the "each review adds a sibling run" activity) while keeping `base/`, the phase-audit reviewer references, the `<artifact>-review-N-rejected.md` / `<artifact>-review-approved.md` artifacts, and the `reference/pipeline-versioning.md` link byte-unchanged. I independently re-swept the entire repository (case-insensitive `review`/`revis` plus targeted run-naming patterns: `review-N`, "review run", "each review", "sibling run", "revise this") and confirmed both halves of the plan: (a) `README.md:157` is the only documentation surface outside the code plan's `skills/ agents/ .rp.md` corpus that narrates the run-naming concept, and (b) every excluded surface is genuinely out of scope. The README's other `review` hits (lines 36, 42, 45 are generic owner-review-of-artifacts; line 112 lists phase-audit reviewer agent profiles) carry no run-naming token, so the single-task scope is right. The CHANGELOG-as-immutable rationale is sound and corroborated by `CONTRIBUTING.md` (the changelog is generated by `@changesets/changelog-github`, and each `## <version>` entry becomes the body of the corresponding GitHub Release — rewriting a shipped entry would falsify the release record). The `pr-description.md` (issue #122) and `website/`, `CONTRIBUTING.md`, `AGENTS.md`/`CLAUDE.md`, the PR template, `.changeset/*`, `scripts/`, `.claude-plugin/`, and `.pi/` exclusions all hold under my sweep.

## Verification performed

- **Worktree confirmed:** `git rev-parse --show-toplevel` ends in `/.claude/worktrees/158-rename-review-runs-to-revisions`.
- **Guardrail scopes:** No guardrails are defined for this project, so the `None | None` row is the correct rendering. No scoped gates exist, so step 2 had nothing to execute.
- **In-scope surface:** A whole-repo targeted run-naming grep (`review[ -]?run|review-[0-9]|review run|revise this|review this pipeline|each review|sibling…run`, excluding `.pipelines/`, `.git/`, `node_modules`) returns run-naming hits only in: the code plan's 8 in-skill files + `.rp.md` (code-plan scope), `README.md:157` (this task), and the deliberately-excluded `CHANGELOG.md:38` / `pr-description.md`. No other in-scope documentation surface exists.
- **README boundary:** `README.md:157` contains exactly the rename tokens Task 1 targets (`review-N-<short-description>/`, "each review adds a sibling … run") plus the keep-tokens Task 1 lists (`base/`, reviewer-agent references, `<artifact>-review-N-rejected.md`, `<artifact>-review-approved.md`). The README links to `reference/pipeline-versioning.md`, not the `git mv`'d `review-pipeline.md`, so the code plan's file rename cannot break the link and `Depends on: none` is correct.
- **Excluded surfaces:** `website/` `review`/`revis` hits are all phase-audit (`spec-reviewer`, `*-review-approved.md`) or generic ("review at checkpoints"/"reviewing") — no run-naming. `CHANGELOG.md` has one run-naming hit (the shipped `## 0.3.0` PR-#106 entry); `CONTRIBUTING.md` confirms the changelog is generated and its entries become GitHub Release bodies, making the immutability exclusion sound. `pr-description.md` is a transient working file for the completed issue #122. `CONTRIBUTING.md`, `AGENTS.md`/`CLAUDE.md`, `.github/PULL_REQUEST_TEMPLATE.md`, `.changeset/*`, `scripts/`, `.claude-plugin/`, `.pi/` show zero run-naming hits.
- **Adjacent in-skill narration:** `skills/radical-pipelines/reference/create-pipeline.md:23` narrates the run-folder model but contains no `review`/`revis` token (names only `base`, delegates the convention to `pipeline-versioning.md`), so it is correctly a surface for neither plan.
- **Code-plan corpus baseline:** live `grep -rni 'review' skills/ agents/ .rp.md` = 252 and `grep -rni 'revis'` = 32, matching the code plan's stated occurrence set; no `revision-N` token already present (clean starting state).

## Checklist findings

- **Guardrail-scopes coverage / bind:** N/A — no scoped gates; the `None` body is the valid rendering.
- **Coverage of surfaces:** Complete. Single in-scope surface correctly identified; all exclusions verified and justified.
- **Traceability:** Task 1 traces to spec requirements 1, 2, 3, 5; the named run-naming acceptance criteria; the phase-audit-preservation invariant; Design "Components"; and code tasks 2–9. All check out.
- **Per-task acceptance:** Five evaluable, drift-resistant criteria framed as what the reader leaves with and what must stay byte-unchanged, consistent with the spec's named-token wording.
- **Drift-resistance:** Stays at what/where/for-whom. The only fixed tokens (`revision-N-<short-description>`, "revision run", `base/`, `*-review-approved.md`) are spec-mandated naming invariants, not drift-prone implementation details.
- **Audience clarity:** "Prospective adopters and contributors reading the top-level README" — concrete.
- **Granularity:** One precision edit to one paragraph — right size for a single docs-writer.
- **Ordering and dependencies:** `Depends on: none` is correct and verified (README link target is unaffected by the code plan's rename).
- **Feasibility:** `README.md`, its run-model paragraph, and the `pipeline-versioning.md` link target all exist.
- **No code planning / scope:** Task 1 is prose-only; no code tasks; stays within spec and design.
- **Clarity and consistency:** Two docs-writers reading the actual paragraph would produce the same scope and shape; the plan agrees with the code plan and design throughout.
