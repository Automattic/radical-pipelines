# Doc Plan Review

## Verdict: approved

## Summary

The doc plan is a single-task plan, and its minimalism is honest. I verified every load-bearing claim against the real repository. The one task — a narrative-coherence pass over the README `## Configuration` section — addresses a genuine documentation gap that the approved code plan deliberately leaves uncovered: code-plan Task 5 inserts exactly one standalone `.rp.local.md` paragraph and explicitly instructs "Do not alter the surrounding paragraphs," deferring narrative refinement to the doc phase. Without this task, README lines 153 and 167 would continue to state conventions live in a "single merged" `.rp.md` and that a project "keeps everything in one merged" file — framing that directly contradicts the new paragraph. This is a real, observable post-phase-4 inconsistency, not a code-phase duplicate and not scope creep. The task is well-formed (Goal / Audience / Files / Scope / Depends-on / Traces-to / Acceptance all present), drift-resistant (outcome-framed acceptance, no wording/function/parameter hooks, anchored by paragraph content rather than brittle line numbers, with the doc-writer reading the actual edited README in phase 5), correctly traced (spec req 20, design-doc "Discoverability touchpoints → 4" / "Modified components → README.md", code-plan Task 5), and correctly sequenced after the code-phase paragraph lands. Every "deliberately not given a task" exclusion held up under direct inspection of the repo.

## Verification performed

I did not take the plan's word for its sweep; I checked each claim against the working tree.

- **README target paragraphs exist as described.** Line 153 ("Conventions live in a **single merged** `.rp.md` file") and line 167 ("A single project **keeps everything in one merged** `.rp.md`") both carry the sole/whole-source framing the task targets. The diagnosis is precise. The task's section-wide acceptance criterion ("no sentence in the section asserting or implying the committed `.rp.md` is the only/whole source") is broader than the two named paragraphs, which is a strength: I confirmed lines 155 and 163 contain only setup-flow / load statements, not sole-source claims, so the two-paragraph **Files** scope and the section-wide **Acceptance** do not contradict each other against the actual file.
- **Clean, non-duplicative seam with the code plan.** Code-plan Task 5 owns the standalone advertising paragraph and the req-20 conventions-overview mention; code-plan Task 1 owns the canonical `local-overrides.md` (including the req-24 assisted-inertness note); code-plan Tasks 2–4, 6–7 own `load.md`, `setup.md`, `SKILL.md`, the dogfood `.gitignore`, and the changeset. The doc task touches none of these functional deliverables — it only reconciles surrounding prose the code plan explicitly leaves untouched. No req-20/req-23 deliverable is duplicated, and no req-20/req-24 doc obligation is dropped (each is already a code-plan acceptance criterion).
- **`website/`** — grep for convention/`.rp.md`/`.rp.local`/gitignore/agent-model/health-monitor/issue-tracker/config returned **zero** matches. The "marketing-only, zero references" exclusion is honest.
- **`agents/*.md`** — every "conventions" hit is generic ("host project's conventions", "follow project conventions"); none enumerates the convention catalog or frames the committed file as sole source, and none names `.rp.md`/`.gitignore`/agent-models/health/Issues access. The overlay contradicts nothing there. Honest.
- **`AGENTS.md`** — a minimalist "rules when modifying the skill" doc with no convention catalog and no `.rp.md`/`.gitignore`/feature reference. Honest. (The README's line-175 pointer to AGENTS.md concerns the changeset rule and is a pre-existing matter unrelated to this feature, correctly out of scope.)
- **`CONTRIBUTING.md`** — classifies `.gitignore` as a non-release-relevant "meta file" (line 75), and lists `skills/**` and `README.md` as release-relevant (lines 56–65). The plan's claim that this classification stays true — because the `.gitignore` edit ships alongside `skills/**`/`README.md` edits that warrant the code-plan changeset — is verified. No CONTRIBUTING statement goes stale. No second changeset is needed for the doc-phase README edit: a changeset is per-PR, the feature is one PR, and code-plan Task 7's changeset already covers `README.md`. Correctly excluded.
- **Dogfood `.rp.md` and root `.gitignore`** — both exist; the `.gitignore` contains exactly `node_modules/`, `.env`, `.env.local` (no worktree-folder line), matching code-plan Task 6 and design-doc scope guard (a). Both are code-plan/design-doc concerns, correctly excluded from the doc plan.

## Notes (non-blocking)

- The line-167 paragraph's primary job is to explain the shared-vs-per-tool structure and the dogfood multi-CLI case; the "keeps everything in one merged" framing is incidental to it. The task already guards this with acceptance criterion 3 ("nothing outside the convention-source framing is rewritten"), so a phase-5 doc-writer is adequately bounded to soften the framing without damaging the structural/dogfood explanation. Sufficient as written; flagged only so the doc-writer keeps the surrounding explanation intact.
