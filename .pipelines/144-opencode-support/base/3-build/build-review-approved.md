# Build Review

## Verdict: approved

## Batch scope

Expected new work: Task 7 (Plugin module — tools, listener, scheduler, and server reach), re-dispatched to resolve the single finding of `build-review-2-rejected.md` (commit `3043555`).
Diff reviewed: `f9740b9^` (= `c351f0e`) → HEAD (the phase's whole work)

## Summary

The iteration-2 finding is resolved and behaviorally proven: the collision unit test now injects `readServiceRecord: () => null, readCliVersion: () => null` (`scripts/test/opencode/plugin.test.mjs:219-220`), matching the file's established stub pattern, and I verified the fix under a simulated owner machine — a fake `$HOME` carrying a poisoned service record (`url` pointing at a connection-refused port), a leftover `loops.json` registry entry, and a logging `opencode2` shim first on `PATH` — where `npm test` stayed 157/157 green with zero `opencode2` invocations and the planted state untouched. All five iteration-1 findings remain resolved (re-swept: no artifact citations in any shipped file, outcome-aware notification text with exclusive-match unit assertions and the updated suite assertion, collision surfacing via `recentErrors` with unit and suite coverage, the `/pending` lingering check green at 6.2s, the vacuous assertion replaced by `assert.deepEqual(result.recentErrors, [])`). I independently re-ran the pinned integration suite (28/28 against CLI `0.0.0-next-15772`) and additionally drove the one topology the suite never exercises — a real `opencode2 service start` daemon — confirming the service-record reach path end-to-end. The fixed gate runs and passes. Accepted residuals, with justification, are recorded below; none is a code defect in the shipped artifact.

## Checks

| Check | Command | Result |
| ----- | ------- | ------ |
| tests | `npm test` (from the worktree root) | pass (157 tests, 0 fail) |

## Behavior verification

- **Hermeticity of the fixed gate (this batch's changed behavior):** built a torture rig simulating the owner machine the iteration-2 finding targeted — `$HOME/.local/state/opencode/service-poison.json` (`{url: "http://127.0.0.1:1", password, version: "0.0.0-next-99999"}`), `$HOME/.local/share/radical-pipelines/loops.json` (one leftover entry, interval 300000), and an `opencode2` shim on `PATH` logging every invocation — then ran `npm test` with that `HOME`/`PATH`. Result: **157/157 pass, invocation log empty, planted files untouched**. The gate is deterministic and offline even with a (stale) daemon record present and the binary installed.
- **Automated flows, independent re-run:** `npm run test:opencode` from the worktree root — **28/28 checks passed** against the pinned CLI (sandbox `serve` at `127.0.0.1:46177`, offline SSE stub), including both iteration-2 additions: "collision reporting: the seeded spec-lead.md collision is observable via rp_status's recentErrors" (940ms) and "an admitted-but-unpromoted message lingers observably in /pending while the target is busy, then drains once it goes idle" (6247ms). All previously-green checks remain green (spawn/seat/ledger/title with the auto-title race, attribution and the 404/pending/structured-error failure chain, loop lifecycle including `serve`-restart re-arm, interrupt, same-session model switch, auth recovery, the network probe, and the pin assertion).
- **Daemon-topology probe (reach path the suite never drives):** in a fresh XDG-isolated sandbox, started a real background daemon (`opencode2 service start`, pinned build) with **no** `RP_OPENCODE_SERVER_URL`/`OPENCODE_PASSWORD` overrides, and verified with the plugin's own code: the daemon wrote `service-*.json` under `$XDG_STATE_HOME/opencode` with `{url: http://127.0.0.1:4096, version: 0.0.0-next-15772, pid, password}`; `readServiceRecordFile` + `resolveServer` (default reader) resolved it; after the first session, `/api/plugin` reported `radical-pipelines@0.10.0`; `/api/agent` listed **all 17 RP agents by their RP names**; and a driven `rp_status` inside the daemon — its reach resolved from the record alone — returned `pluginVersion=radical-pipelines@0.10.0, pin=match`. Transcript tail: `DAEMON PROBE: ALL CHECKS PASSED`. Daemon stopped and sandbox destroyed afterwards.
- **Regression sweeps:** artifact-reference sweep (`flow N`, `build plan`, `acceptance criteri`, `spec.md`, `design doc`, `.pipelines`, `task N`, `requirement N`) over `opencode/`, `scripts/opencode-integration/`, `scripts/test/opencode/`, the skill prose, the changeset, and `package.json`: no matches. `completed its first turn`: no matches anywhere; the suite asserts the outcome-aware text (`spawn-and-messaging.mjs:90`). Suite headers describe mechanics in repo-standalone terms.
- **Skill-surface prose (static):** `setup.md` gains exactly the opencode Tool→Read row; `load.md`'s rule is tool-agnostic and folds a mismatch into the existing missing-conventions stop/explain/offer-setup flow, preserving behavior when the section matches (this repo's own `.rp.md` matches under Claude Code); `opencode.md`'s canonical blocks name exactly the shipped `rp_*` tools and arguments, the fixed seat, the session-ID identifier, the worktree-root constraint, and the plugin-installed Setup-actions check; `claude-code.md` and all other skill files are untouched by the diff.
- **Unit evidence:** all eight `scripts/test/opencode/*.test.mjs` files re-read line-by-line; every test is offline (temp dirs, injected env/readers/request functions, fake `ctx`); each per-task Acceptance bullet of Tasks 1–8 has a matching passing assertion.

### Accepted residuals

- **Two phase commit bodies reference planning artifacts** (`881cf28`: "Wires the Task 2-6 helpers into setup(ctx)"; `666a791`: "citing the build plan's flow numbering"). Shipped files are fully clean; only these history-recorded messages violate the software-only-output rule's letter. The sole remedy is rewriting ~15 shared-branch commits, which would invalidate the commit SHAs cited by the committed review artifacts of iterations 1 and 2 and destabilize the run branch other agents track — net damage to the run's inspectable record exceeding the blemish. Accepted as residual rather than re-dispatched.
- **Unit tests calling `setup` with `env: {}` let the loop re-arm read the real `~/.local/share/radical-pipelines/loops.json`** (read-only; timers armed at owner-scale intervals and cleared per-test). Empirically verified harmless: the torture rig planted a real leftover entry and the gate stayed green with the file untouched. No network, exec, or write boundary is crossed.
- **Manual smoke flows not drivable in this phase's environment:** the github-specifier channel install/update requires a release tag that will not exist until release (the release-cadence network-smoke path deliberately excluded from the in-phase run), and the conversational flows (setup dialogue, mismatch dialogue, monitor-driven recovery choreography, full five-phase run, resume) require a live owner orchestrator session with real provider credentials. Every mechanic these flows compose is individually verified against the pinned build (28/28 suite + daemon probe + units), and their prose surface is statically verified. Verified later by: the release-cadence `--network-smoke` suite path and the owner's first pinned install and run following the Document phase's README procedures. Deferral is safe because the unverified layer is conversational composition over mechanics proven here.
