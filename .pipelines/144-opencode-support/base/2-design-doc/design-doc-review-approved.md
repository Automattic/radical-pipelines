# Design Doc Review

## Verdict: approved

## Reviewed revision

93e3bf7e2c9582cd0347084d4a43170b1e084595 (`Address review round one: spawner field, aligned spawn contract, notification policy, network-error scope (design-doc-lead)`), branch `144-opencode-support`.

## Verification log

- Change scope since round 1 — `git diff 15b4675..HEAD --stat` → only `design-doc-research.md`, `design-doc.md`, and the round-1 review file changed; `skills/`, `agents/`, `README.md`, `package.json`, `scripts/` untouched → every round-1 codebase-side check remains valid and is reused below.
- Spec baseline — `git log df7c532..HEAD -- 1-spec/` → only `cbd0714` (adds `spec-review-approved.md`); `spec.md`/`spec-research.md` unchanged since approval → record's baseline holds (reused from round 1, re-confirmed).
- Full revision diff — `git diff 15b4675..HEAD` on both artifacts, read line by line → every change maps to exactly one of the four round-1 issues or the new Review Adjudications section; no unrelated edits, no silently dropped content.
- Issue 1 fix (spawner) — record Topic 3 decision + correction, doc `rp_spawn` bullet + Completion notifications → ledger entry is `sessionID → {name, run, spawner}` with `spawner` = caller's `toolCtx.sessionID` (unspoofability verified live in Topic 4, unchanged); restart behavior now explicit (map and in-flight executions die with the daemon; post-restart terminal event resolves no spawner, no notification) and consistent with the git-based resume model; the title's deliberate spawner omission is justified (post-restart there is never a notification to route) → the declared interface now delivers the declared behavior. Resolved.
- Issue 2 fix (contract drift) — Topic 3 now includes `run`; `agent` (profile name, validated) and `name` (run-unique instance name) defined once and glossed consistently in Topic 8 and both doc sections; map and title keyed by `name` (`rp:<run>:<name>`); Topic 5 declares `target_session` optional-with-caller-default, matching Topic 8 and the doc. Leftover-notation grep (`agent-name`, `{name, run}`, `logical name`, `run-branch`) → only rejected-alternative lines and one equivalent doc gloss (`rp:<run-branch>:<instance-name>`, same defined values) remain → no section pair keys the ledger differently. Resolved.
- Issue 2 premise re-execution — grep `skills/` → "Give the agent a name unique within the run" verbatim at `autonomous-workflow.md:66` (which also allows the identifier to be "returned by it per the Team spawning convention" — rp_spawn's returned session ID fits without skill change); multi-instance profiles confirmed as the norm: both phase flows re-launch "a fresh reviewer" per rejection round (`1 - spec.md:36-37`, `2 - design-doc.md:45-46`), so a title keyed by profile name would indeed collide → the `name`-keying rationale holds.
- Issue 3 fix (notification policy) — policy decided (first terminal event only), recorded in Topic 3, the adjudication, and a dedicated doc decision with alternatives weighed and "Traces to: Requirement 11; supports 14". Cost premise re-executed from retained artifacts: `/tmp/oc-sandbox/L/loop.log` → injection into an idle target at 20:49:28 left it busy at the 20:49:32 tick (idle injection starts a full execution); 4 consecutive `SKIP (busy)` ticks then exactly one `INJECTED` matches the record verbatim; `/tmp/oc-sandbox/L/msg.log` → every execution ends with `session.execution.succeeded`/`.failed` → the every-terminal-event-notifies cost argument is corroborated. Fallback premise verified: "Agents message you when their work completes." verbatim at `autonomous-workflow.md:71`. Req 11's "notification when it completes" is delivered by the first terminal event of the spawned task; later-turn failures remain observable via the listener's error log, `rp_status`, and the durable HTTP observables the monitor polls — req 14 unbroken. Resolved.
- Issue 4 fix (network-error scope) — Topic 7 observable 4 re-scoped to provider-call failures (matching what `provider.auth`/`provider.no-route` evidence establishes); tool-call failures observed via the Claude Code-parity signals (agent report, worktree state, messaging state, stall backstop — the health-monitoring.md contract verified in full in round 1, file unchanged); open question extended to the channel itself with a named Build-phase verifier (forced tool-call network failure) and a channel-independent recovery action; doc splits the failure bullet accordingly. The residual "ordinarily" is covered by that open question's named verifier and safety argument. Resolved.
- Adjudication record — "Review Adjudications — Round 1" in the record → describes exactly the changes the diff shows, all four adopted, no refutations, no residuals → accurate.
- Fidelity — doc vs record on every changed section → agree on parameter lists, ledger shape, title key, notification policy, restart semantics, loop tool signature, and the network-error split. Two cosmetic glosses noted, neither load-bearing: the record has `rp_spawn` return the session ID "plus the instance name" while the doc names only the session ID (nothing consumes the echoed name — the caller supplied it); the doc's mental-model title gloss uses the descriptive spellings of the same defined values.
- Internal consistency of the revised contract — doc "Spawn failures" (first-turn `execution.failed` reported to the spawner) is consistent with the first-terminal-event policy; Topic 8's canonical block ("notified when a spawned agent's first turn completes") is a faithful owner-facing gloss of the same policy; all terminal events still feed the listener's records (error log, title re-assert) as req 14 needs.
- Sender-attribution edge — checked: an unledgered sender (the orchestrator, never spawned) has no resolvable `name` for the `[from <name> (<sid>)]` prefix; the load-bearing element — the embedded sender session ID the receiver replies to, taken from unspoofable `toolCtx` — is fully specified, so the reply path and req 12 are unaffected. Observation only, not a finding.
- Hedge scan — grep likely/probably/presumably/assumed/might-be/should-work over both artifacts → no hits; each open question names its verifier and why deferral is safe.
- Coverage, traceability, scope, altitude — reused from round 1 (walked all 16 requirements, all acceptance criteria, Out of Scope 1–7; every decision carries "Traces to"; no scope creep; architecture-level throughout): the revision removes no coverage and adds one traced decision → still complete.
- Empirical corroborations reused from round 1 (claims and sandboxes unchanged since 15b4675): pinned-build re-execution (`opencode2 --version` → 15757), zero-dep plugin smoke, auth-error shape (`provider.auth` with verbatim 401 body), ledger-field capture (`create1.json`, `asg.json`), npm channel/pin checks.
- Limitation (carried from round 1) — the full live opencode stack (serve, spawn, loop, messaging end-to-end) was not re-driven; those checks mutate daemon/XDG state. Corroboration relied on the researcher's retained sandbox artifacts and logs plus the re-executions above; the design itself routes final confirmation through the pinned integration suite (req 3).
- Worktree clean before writing this review — `git status --porcelain` → empty.

## Summary

The revision resolves all four round-1 findings exactly as adjudicated, and the fixes hold up under fresh checks: the spawner field makes the completion-notification chain deliverable by the declared interface, the spawn/ledger contract is now stated once and consistently across every section, the notification policy is a recorded decision whose cost argument I corroborated against the retained live logs and whose fallback protocol exists verbatim in the skill, and the network-error observability claim now says precisely what the evidence establishes, with the residual routed to a named Build-phase check. No new inconsistencies were introduced; the doc faithfully reflects the amended record; coverage of all 16 requirements, traceability, and scope discipline carry over intact from round 1. The design rests on live-verified primitives of the target build throughout, its open questions are properly deferred with named verifiers, and two implementers reading the doc independently would now build the same coordination layer.
