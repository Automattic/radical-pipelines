# Spec Review

## Verdict: approved

## Reviewed revision

df7c532 — "Address review round one: cover update and full recovery matrix in acceptance criteria (spec-lead)".

## Verification log

- Change surface since round 1 — `git diff cbee2b9..df7c532 --stat` → exactly three files: `spec.md` (+3 acceptance criteria), `spec-research.md` (+Review Adjudications), `spec-review-1-rejected.md` (the rejection itself). No skill, agent, or packaging file changed, so round 1's codebase checks remain valid where reused below.
- Round 1 Issue 1 resolution — diff of `spec.md`: three criteria added (update procedure; message-failure re-send → restart; network-failure retry once → wait one interval and retry) — matches the rejection's suggestion and the adjudication recorded in `spec-research.md`; requirements and record correctly left unchanged, since the gap was spec.md coverage only.
- New ACs vs recovery table — read `health-monitoring.md`: message-failure signal ("failed, errored out, or was never delivered", :20) and actions (re-send → restart target, :33); network-failure signal ("a tool call failed with a transient network error", :22) and actions (retry once → wait one interval and retry → escalate, :35) — both new criteria match the table verbatim, including escalation ordering.
- AC ↔ requirement cross-check (fresh, full) — every requirement 1–16 now covered: 1→AC1, 2→AC1+16, 3→AC16, 4→AC2, 5→AC3, 6→AC1 (invokable) + AC14 (e2e exercises the skill tree), 7→AC1, 8→inspectable structural constraint + AC17, 9→AC4, 10→AC5, 11→AC6, 12→AC7, 13→AC8-9, 14→AC10-13 (all four signals, all five recovery actions incl. identifier propagation in AC10), 15→AC14, 16→AC15; exclusion 7→AC17. Every AC traces back to a requirement or exclusion.
- Requirement 9's convention split — read `load.md`: required = Branch name base, Pipeline family folder, Issues, Worktree root, Team spawning, Health monitoring, Artifact storage; optional = Commit format, Agent models, Guardrails — matches the record and requirement 9 exactly.
- Requirement 10's grounding — `load.md` "Missing conventions" checks presence only, no tool-match guard; setup skipped when required conventions present — negative-space gap confirmed by fresh read.
- Requirement 13's parity contract — read `claude-code.md`: `/loop <interval> <prompt>` start, `CronList` list, `CronDelete` cancel, canonical Team spawning block with seat-by-cd and identifier-from-spawn-result — confirmed; grounds requirements 8, 9, 11, 13.
- Requirement 16's grounding — read `resume-pipeline.md`: leftover loop persists until cancelled (:9), worktree reuse/recreate from branch (:13), commits and diff the only record of progress (:23) — confirmed.
- Research item 1 (versions, external re-check) — `npm view`: `opencode-ai@latest` = 1.18.3 (bin `opencode`); `@opencode-ai/cli@next` = 0.0.0-next-15757 (bin `opencode2`) — the `next` tag has moved 15718 → 15756 → 15757 across three days, confirming the moving-beta premise behind requirements 2–3.
- Adequacy of open upstream limitations — record's noted open issues (#36605 fork/location, #34957 restricted plugin session subset, #36349 completion durability across restart) constrain design mechanisms, not the outcome-level requirements: requirement 11's completion notification is in-session, and cross-restart survival is excluded by exclusion 6 (git-based resume). No requirement rests on an unresolved premise.
- Residuals — the one open uncertainty (git specifiers through the npm resolver, research item 7) is explicitly recorded, the affected requirement (1) is outcome-level, and the channel choice is dispositioned to design with a mandated empirical spike — a properly recorded residual, not an unverified hedge.
- Reused from spec-review-1-rejected.md (valid — checked files unchanged since cbee2b9 per the diff check above): Q1 packaging/install facts (plugin.json, marketplace.json, package.json, sync-version.mjs, 17 agent profiles, plugin cache); setup.md line citations (Tool→Read table, inform-not-ask, cross-session spawning, Setup actions, commit + gitignore, skip-when-complete); phases and completion predicates (SKILL.md, pipeline-versioning.md); close-out steps (autonomous-workflow.md:89-95); four liveness signals and 2-retry table (health-monitoring.md — also freshly re-read above); negative greps (permissions, overlap/concurrent); idle-fire property from the bundled loop SKILL.md; Q2 owner quotes on issue #144 (v1-first superseded, pin + autoupdate false, compatibility matrix + pinned integration tests); research items 2–7 verified at source level against the v2 docs and `v2`-branch tree; intent coverage trace; fidelity of requirements and Out of Scope to the record.
- Worktree clean before writing this review — `git status --porcelain` empty at df7c532 — confirmed.

## Summary

Round 1's single finding is fully resolved: the three added acceptance criteria close the coverage gap precisely — the update procedure is now exercised, and requirement 14's recovery matrix is covered signal-for-signal and action-for-action against the skill's recovery table, whose wording the new criteria match verbatim. The adjudication is honestly recorded, and nothing else changed, so round 1's extensive verification carries forward intact. My independent pass — fresh reads of the skill files grounding requirements 8–11, 13, and 16, a full AC-to-requirement cross-check, an external re-check of the opencode version claims, and an adequacy audit of the open upstream limitations — found no new issues: every requirement and exclusion traces to recorded research, the two premise corrections carry no dependent requirements, altitude is held (mechanisms consistently dispositioned to design), and the intent's goals, constraints, and assumptions are all served or explicitly dispositioned. The spec is ready for the design phase.
