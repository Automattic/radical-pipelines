# Code Review

## Verdict: approved

## Batch scope

Tasks reviewed:

- Task 1: Author the canonical Rule 1 + Rule 2 block and land it in `code-writer-tdd`, replacing the narrower Rule 2 line (`agents/code-writer-tdd.md`)
- Task 2: Copy the canonical block into `code-writer-e2e` and add the product-commit constraint (`agents/code-writer-e2e.md`)
- Task 3: Copy the canonical block into `docs-writer` and add the product-commit constraint (`agents/docs-writer.md`)
- Task 4: Add the canonical block plus enforcement to `code-reviewer` (`agents/code-reviewer.md`)
- Task 5: Add the canonical block, enforcement, and a standalone commit-message inspection item to `docs-reviewer` (`agents/docs-reviewer.md`)

## Summary

The batch ships exactly what the approved plan and design call for and nothing more: prose-only edits to the five host-product agent profiles, with no runtime code, no test files, and no change to any earlier-phase profile, skill, convention, or phase reference. The single canonical Rule 1 + Rule 2 block is byte-identical across all five profiles (verified by SHA256 — every copy is `6cc5029c…`, 1972 bytes), satisfying Req 11's "stated once and consistently" under the repository's duplication-over-reference constraint. The Rule 1 prose keys on the touched-vs-untouched axis, names the forbidden action and the permitted case, disclaims any preserve-a-still-valid-comment duty, and exempts commit messages; the Rule 2 prose is referent-based, spans the full content surface, carries concrete "NOT a violation" negative examples including the self-hosting carve-out, and explicitly refuses a token/keyword/path scan. The pre-existing narrower Rule 2 line is deleted and exists nowhere in `agents/` or `skills/`. Each producing profile's commit step gained the format-agnostic no-provenance constraint; the two reviewer profiles gained the same block plus a must-fix enforcement item (riding the existing Convention-compliance commit hook in `code-reviewer`, and a distinct standalone commit-message-provenance item in `docs-reviewer`), with both reviewers' own commit steps left untouched. The batch's own five product commits dogfood the rule: each is a product commit by the changed-path test and each is correctly untagged, while the run's artifact-only commits keep their provenance tags — the contrast that proves the changed-path classifier is honored. There is no guardrails convention for this project (the plan records `None`), so per the profile the step-2/3 inspection judgment is authoritative; the existing test suite was run only as a regression sanity check and passes (57/57).

## Checks

| Check | Command | Result |
| ----- | ------- | ------ |
| Guardrail gate | None (no guardrails convention; plan "Guardrail scopes" records `None / None`) | skipped |

Note: there are no declared guardrail gates, so none were run. The project's `package.json` carries a `test` script (`node --test 'scripts/test/**/*.test.mjs'`), but it is not a declared guardrail gate and the plan authoritatively records no guardrails for this project; it was executed only as a non-gating regression sanity check (57 pass / 0 fail) and is not counted as a gate above.

## Behavior verification

This feature ships prose-only edits to Markdown agent profiles — no runnable behavior and no automated test (a structural assertion over agent-file content is forbidden by `CLAUDE.md:17` / `AGENTS.md:17`). Acceptance was verified by direct inspection of the shipped prose, re-driving the plan's six inspection flows. Evidence:

- **Flow 1 (both rules present, always-on, no opt-out).** All five profiles carry the canonical block. A repo grep across the five profiles for `opt-out|override|toggle|disable|owner action|per-run|per-project|unless the owner|you may skip` returned nothing. No profile offers any mechanism to disable, override, or opt out of either rule. PASS.
- **Flow 2 (Rule 1 — untouched content; naturally-updated comments permitted; commit messages exempt).** The canonical Rule 1 prose: keys on "the edit boundary"; forbids "reword, reflow, reformat, or tidy a comment attached to code your change does not modify, or a prose section of a documentation file your change edits but does not otherwise touch — leave it exactly as it was"; permits "Updating a comment or prose that belongs to content your change *is* modifying"; disclaims duty ("You have no duty to preserve a still-valid comment that sits beside code you changed — you may keep it, rewrite it, or drop it"); gives a concrete NOT-a-violation example ("leaving a comment two functions away from your edit exactly as you found it"); and states "Rule 1 does not apply to commit messages." Maps to spec AC under "Rule 1" (lines 65–68). PASS.
- **Flow 3 (Rule 2 — total content reach; referent-based discriminator; negative examples; self-hosting carve-out).** The preamble names the full surface ("comments, identifiers and names, string literals, log and error messages, and inline API documentation"). The decisive one-line test is referent-based ("write about the subject matter of the product, never about the process that produced it"; "violates Rule 2 only when it identifies the concrete pipeline run that produced this output — naming its phases, artifacts, plan tasks, or agents as the authors of this work — or narrates your own process"). Concrete NOT-a-violation examples: a `spec.md` filename literal, an illustrative `.pipelines/<slug>/…` path, and the self-hosting Radical Pipelines repository's legitimate vocabulary/methodology/artifact-type/illustrative-path use. It explicitly refuses a checklist ("do not screen for tokens, keywords, or paths"). Maps to spec AC under "Rule 2 — content" and "Rule 2 — referent-based discriminator" and Out-of-Scope #2. PASS.
- **Flow 4 (product commit messages carry no pipeline-naming provenance, format-agnostically).** All three producing profiles' commit steps carry the byte-identical clause "using the host project's commit format, but omit the pipeline-naming provenance: no agent-name tag, and no naming of any phase, artifact, or plan task" (`code-writer-tdd.md:53`, `code-writer-e2e.md:45`, `docs-writer.md:57`) — it defers to the host format and forbids one property, so it neither depends on nor alters the host's specific format. Both reviewer profiles' own commit steps (step 6) are unchanged and retain the full host commit format including the provenance tag (the only diff hunks in each reviewer are confined to step 2). Maps to spec AC under "Rule 2 — commit messages and provenance". PASS.
- **Flow 5 (enforcement — a violation is a must-fix issue at each product-producing gate).** `code-reviewer.md` step 2 carries the canonical block plus a must-fix "Output rules" checklist item (line 36) applying the referent discriminator, and the product-commit no-provenance check rides on its existing "Convention compliance" item, which now names the changed-path test (line 37). `docs-reviewer.md` step 2 carries the canonical block, a must-fix "Output rules" item (line 39), and a distinct standalone "Product-commit-message provenance" item (line 40) separate from its documentation-only "Convention compliance" item (line 38). Both route a found violation through the existing reject-liberally / every-issue-is-must-fix machinery; no new gate, no bypass. Maps to spec AC under "Enforcement". PASS.
- **Flow 6 (single source — one canonical wording, byte-identical, no surviving narrower variant).** The extracted canonical block from each of the five profiles hashes to the identical SHA256 (`6cc5029c760f3ee9890e2e67ef0263720bb2c4dcdf975d3d96b0b992126d56cf`, 1972 bytes); pairwise `diff` against `code-writer-tdd` reports IDENTICAL for all four other profiles. The pre-existing narrower line "Comments must be self-contained — never reference the spec, the plan, or any other artifact." (present at base `code-writer-tdd.md:33`) returns no matches in `agents/` or `skills/`; the remaining "self-contained" hits denote unrelated concepts (task-block / intent-file self-containment), not a Rule 2 statement. Maps to spec AC under "Single source and consistency" (Req 11). PASS.

**This batch's own commits (Req 7 / Rule 2 dogfooding).** The five product commits (`5a0cdb1`, `43b29c3`, `f3582b8`, `b91a3ab`, `d51b513`) each change exactly one path under `agents/` — all outside `.pipelines/`, so each classifies as a product commit by the changed-path test. None carries an agent-name/pipeline-naming provenance tag; subject lines name the edited profile file (the edit's subject matter), not the producing agent of this run, which is the referent-based discriminator working as designed. The `Co-Authored-By: Claude` trailer is a host-harness attribution trailer that names no pipeline, phase, artifact, or agent of this run, so it is not a Req 7 violation. By contrast, sampled artifact-only commits in the same range (`34a9227`, `e4d746e`, `535bdda`) — all paths under `.pipelines/` — retain their `(<agent-name>)` provenance tags, exactly as Req 8 prescribes. Tagged product commits would be the violation; the untagged product commits are correct.

**Scope and regression.** The product diff touches only the five named profiles (one per task; 32 insertions, 5 deletions total), adds no test file anywhere, and leaves every earlier-phase profile, skill, convention, `.rp.md`, and phase reference untouched — consistent with the design's "Untouched but relevant" list and free of scope creep. The existing test suite (release/version/changeset tooling; none asserts agent-file content) passes 57/57, confirming no regression.
