# Docs Plan Review

## Verdict: approved

## Summary

The revised plan fully resolves the single issue from `docs-plan-review-1-rejected.md` and holds up under an independent end-to-end re-review. The prior rejection turned on one factual defect: the website was excluded on a false premise ("its illustrative commit-log entries are all artifact-only commits"), leaving the one product-commit depiction in `website/demo.js` unexamined and contradicting the feature's own Requirement 7. The revision adds Task 3, which targets exactly that surface, and rewrites the out-of-scope note so it no longer makes the false claim — it now correctly states that `index.html`'s `git log` is taken over the artifacts folder (all artifact-only, legitimately tagged per Req 8) and explicitly carves the `demo.js` product commit out to Task 3 rather than excluding it. I verified every load-bearing claim against the worktree, and the surfaces, paths, audiences, and traceability all check out.

I confirmed against the codebase:

- **Task 3 / `website/demo.js`** — the phase-4 step at line 98 writes `src/orchestrator.ts` and `src/orchestrator.test.ts` (product files, outside the artifacts folder) and line 103 depicts `git commit -m "Add orchestrator (code-writer-tdd)"`, carrying the `(code-writer-tdd)` provenance tag. By the changed-path test this is a product commit, so the tag contradicts Req 7 after the feature ships. This is the demo's only `bash`/commit depiction and its only product-file step; every other phase writes artifact files only. Task 3 identifies this surface correctly and keeps its acceptance drift-resistant (it has the writer apply the changed-path test rather than hard-coding the string or line number).
- **Out-of-scope note** — `website/index.html` lines 126–130 are a `git log --oneline .pipelines/issue-1234/base/` whose entries all write only artifact paths, so they correctly keep the tag (Req 8) and are correctly excluded; the note now describes them accurately and distinguishes them from the in-scope `demo.js` product commit.
- **Task 1 / changeset** — `agents/**` is in `changedFilePatterns` (`.changeset/config.json:12`), making the change release-relevant and the changeset mandatory; package is `@automattic/radical-pipelines`; pre-1.0 (version `0.4.0`) feature maps to `minor` with no `BREAKING:` prefix (`CONTRIBUTING.md` lines 110, 122); the existing `unify-docs-naming.md` changeset confirms the front-matter/body shape. `CHANGELOG.md` is generated and correctly excluded.
- **Task 2 / README** — the "What this unlocks" section (README line 40) with "Compounding quality" (line 43) and "Consistent assets" (line 44) exists exactly as described; `README.md` is release-relevant, so the edit is covered by the Task 1 changeset and needs no own changeset; the README's lone "commit format" mention (line 159) is a list item naming `.rp.md` contents and correctly stays untouched.
- **Excluded surfaces** — the `.rp.md` "Commit format" convention is at lines 49–59 (the host format the feature preserves, never alters) and is correctly excluded; `CONTRIBUTING.md:242`'s `git commit -am "Version Packages"` is the release bot's commit (no provenance tag, not pipeline product) and is not a surface; `SKILL.md`/`AGENTS.md`/`agents/**` are tool instructions/code owned by the code phase.
- **Guardrail scopes** — `.rp.md` defines no guardrails, so the `None`/`None` rendering is the valid one.

A whole-repository sweep for output-quality prose, comment-handling prose, and product-commit-provenance depictions outside `agents/`, `skills/`, and `.pipelines/` surfaced no reference the plan leaves out of sync after phase 4. The tasks are traceable, name concrete audiences, stay at what/where/for-whom altitude, carry evaluable drift-resistant acceptance criteria, and plan no code. Approved.
