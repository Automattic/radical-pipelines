# Design Doc Review — Approved

Verdict: **approved**.

Reviewed `2-design-doc/design-doc.md` against `1-spec/spec.md` and `2-design-doc/design-doc-research.md`, with every quoted passage verified against the current working tree.

## Checks performed

- **Requirement coverage.** All 13 spec requirements and 11 acceptance criteria are addressed. Decisions 1–9 map one-to-one onto edit requirements 1–9 and trace explicitly to their acceptance criteria. The cross-cutting requirements — 10 (no meaning lost), 11 (no new violations), 12 (existing branch/PR, no new changeset, Changeset Gate green), 13 (manual verification) — are carried by the per-decision meaning checks, the Failure Modes section, the three-layer verification in Observability, and the Dependencies section.
- **Factual accuracy.** Every current-text quote in the design was checked against the working tree and matches exactly: `fork-pipeline.md:34/38/42–43`, `review-pipeline.md:3/14/16/18/29/31/48`, `pipeline-versioning.md:21–28/51/65/87/115`, `work-on-an-issue.md:36–40`, `autonomous-workflow.md:37`, `README.md:157`, `.changeset/pipeline-reviews.md`, the `autonomous-phases/4 - code.md:35` and `5 - docs.md:36` parentheticals, and the five `AGENTS.md` rules. Decision 4's "last line of the file" claim and Decision 7's context-integrity claim both hold.
- **Consistency with the research record.** Each decision matches the corresponding topic's decision and exact replacement wording (Topics 1–8 → Decisions 1–3, 4 covers Decisions 4 and 7, 5–8 → Decisions 5, 6, 8, 9; Topic 9 → the verification design). The two declined refinements (widening step 6's "Model" citation; further reducing the residual `review-N` clause overlap) and all three risks carry over intact, including the flagged-not-missed "Model"-citation gap.
- **Scope discipline.** The design touches only the nine named sites across six files, leaves the spec's untouchable already-compliant patterns byte-identical (the 4/5 parentheticals, `intent-format.md`, the canonical **Reviewer base ref** rule), edits the changeset side only for finding 6c, and keeps the front matter byte-identical so the Changeset Gate stays green structurally.
- **Soundness of the load-bearing arguments.** The two structural facts the fixes rest on were re-verified: `fork-pipeline.md` is the canonical in-path home for the seeds-only-from-`base/` fact, and the tree-reconstruction section's external entry points (`work-on-an-issue.md:19`, `fork-pipeline.md:9`) do bypass `pipeline-versioning.md:65`, which is why Decision 3 correctly argues operational sufficiency rather than path coverage. Decision 6's timing-drop safety argument (step order in `review-pipeline.md` makes the intent commit the first HEAD-moving action) checks out against the procedure. The retained negatives (review-pipeline opener's "never creates a new pipeline", step 1's "never gate", step 3's two overrides) are correctly classified as load-bearing or out-of-cluster per the spec's own carve-outs.
- **Standalone executability.** The doc is implementable without the research record: exact surviving wordings, deletion boundaries, re-anchoring guidance for the two multi-edit files, and a verification checklist derivable from the decisions.

## Note (non-blocking)

Decision 9 states the only residual README ↔ changeset overlap is the `review-N-<short-description>` clause; the fragments "the original run" and "never rewritten" also survive in both texts. This does not affect the verdict: acceptance criterion 9's bar is sentence-level near-verbatim sharing, which the reworded changeset breaks (different subject, verb, and structure), and requirement 10 obliges both documents to convey the same run-model facts, so some fragment-level overlap is forced. The implementer need not change anything; the cross-cutting re-read should simply judge criterion 9 at the sentence level as the spec defines it.
